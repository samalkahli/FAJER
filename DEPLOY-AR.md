# تركيب نسخة FAJER المحسنة

هذه نسخة محلية جاهزة للمراجعة، وليست منشورة على GitHub أو Vercel. لم يتغير أي منتج أو صورة أو إعداد في Firebase أثناء العمل.
مبنية على commit: 8b42b76335d566733bee607fc90c7b66b4456557.

## 1. احتفظ بنسخة للرجوع

- احتفظ بمجلد المشروع الحالي وبقواعد Firestore الحالية قبل أي استبدال.
- اختبر على فرع جديد وVercel Preview أولا. نسخ الملفات لا يغير قاعدة البيانات، لكن عمليات الإضافة والحذف من لوحة Preview تستخدم نفس قاعدة الإنتاج! اختبر بحذر بمنتج تجريبي فقط أو استخدم مشروع Firebase تجريبي منفصل.
- لا تخلط ملفات JavaScript الجديدة مع HTML القديم؛ ربط الأزرار أصبح في ملفات bindings المنفصلة.

## 2. راجع صلاحيات Firebase أولا

يوجد ملف firestore.rules.template، وهو قالب وليس قواعد منشورة.

1. افتح Firebase > Authentication > Users وانسخ UID الحسابين المصرح لهما.
2. استبدل REPLACE_WITH_FIRST_ADMIN_UID وREPLACE_WITH_SECOND_ADMIN_UID في القالب.
3. تأكد أن نفس المعرفين موجودان في Vercel ADMIN_UIDS مفصولين بفاصلة.
4. إذا كان مشروع Firebase يخدم تطبيقات أخرى أو مجموعات بيانات إضافية، لا تستبدل القواعد كاملة قبل مراجعتها؛ القالب يسمح بقراءة مجموعات المعرض الست فقط، وبالكتابة لمديري المتجر فقط، ويمنع الباقي.
5. جرب Rules Playground: زائر يقرأ products؛ زائر لا يكتب؛ حساب عادي لا يكتب؛ المدير يستطيع الكتابة؛ _uploadLimits لا يقرأه ولا يكتبه عميل المتصفح.
6. بعد التحقق انشر القواعد. لا تنشر القالب بقيمه الافتراضية لأنها ستمنع كتابة المديرين.

UID ليس كلمة مرور ولا مفتاحا سريا. لا ترسل كلمة المرور أو قيمة FIREBASE_ADMIN_PRIVATE_KEY.

## 3. إعداد Vercel

احتفظ بالقيم الحالية في Environment Variables، ولا تنسخها إلى ملفات المشروع:

- IMGBB_API_KEY
- FIREBASE_ADMIN_PROJECT_ID
- FIREBASE_ADMIN_CLIENT_EMAIL
- FIREBASE_ADMIN_PRIVATE_KEY
- ADMIN_UIDS

إضافة اختيارية: ALLOWED_ORIGINS، للنطاقات الإضافية الموثوقة مفصولة بفواصل، مثلا رابط Preview الكامل بدون شرطة مائلة نهائية. لا تستخدم نجمة للسماح لكل المواقع.

- يتطلب الخادم Node.js 22؛ ملف package.json يحدد 22.x. راجع إعداد نسخة Node في Vercel إذا كان لديك اختيار سابق مختلف.
- إعداد Framework هو مشروع ثابت مع Vercel Functions، وليس Next.js. احتفظ بجذر المشروع الذي يحتوي index.html وapi/ وpackage.json.
- لا تضبط Output Directory على مجلد لا يحتوي ملفات الموقع.
- رفع الصور محمي بحد 60 طلبا في الدقيقة و500 طلب في اليوم لكل مدير، ويُحسب اليوم بتوقيت UTC. الحد لا يغطي جميع القراءات العامة إلى Firestore.
- أول عملية رفع تُنشئ تلقائيا مستند عداد في _uploadLimits بواسطة حساب الخادم. لا تنشئه من المتصفح، ولا تفتح صلاحياته العامة.
- يحتاج حساب الخدمة في FIREBASE_ADMIN_CLIENT_EMAIL صلاحية Firestore على مشروعك. إن ظهر خطأ 503 لحد الرفع، راجع IAM ولا تعالج المشكلة بفتح Firestore للجميع.
- كل طلب رفع مصرح به يستخدم معاملة لعداد الرفع؛ هذا يضيف قراءة وكتابة بسيطة إلى استهلاك Firestore.

## 4. استبدال الملفات

انسخ محتويات مجلد FAJER-theme إلى جذر المشروع مع الحفاظ على المجلدات:
api وlib وjs وcss وassets، والملفات index.html وadmin.html وlogin.html وpackage.json وpackage-lock.json وvercel.json.

احتفظ بملفات tests وملفات الشرح في المستودع إذا أردت. .vercelignore يستبعدها من النشر.
ملف css/store-header.css القديم لم يعد مطلوبا؛ أُزيل مرجعه من HTML، ولا يلزم حذفه لتعمل النسخة.

اختبار محلي:
npm ci
npm test

Live Server يشغل الواجهة فقط ولا يشغل الدخول الإداري أو رفع الصور. النسخة الجديدة تتصل حصرا بمسارات /api على نفس نطاق الصفحة. ارفع محتويات الحزمة إلى جذر المشروع وانشر على Vercel ثم افتح login.html من رابط النشر. للتطوير المحلي استخدم Vercel dev من جذر المشروع مع متغيرات البيئة المطلوبة، وافتح الرابط الذي يطبعه بدلا من منفذ Live Server. لا يتم تجاوز فحص صلاحية المدير محليا.

## 5. فحص Preview قبل النشر العام

- صفحة اختيار المتاجر تظهر بنفس الهوية والشعارين.
- افتح كل متجر، ثم الأقسام والفلاتر والمنتج وصور المنتج والآراء.
- بدّل بسرعة بين المتجرين، وجرّب سهم العودة وزر الرجوع في المتصفح.
- جرب الجوال والكمبيوتر، وتأكد أن Console لا يعرض مخالفات CSP أو 404.
- سجل الدخول بكل مدير. حساب غير موجود في ADMIN_UIDS يجب ألا يدخل الإدارة.
- أضف منتجا تجريبيا في كل متجر، ثم عدل صوره وتصنيفه.
- تأكد أن تغيير اسم قسم إلى اسم موجود يُرفض.
- قسم يحتوي منتجات لا يُحذف قبل نقلها أو حذفها.
- حذف قسم فرعي ينقل المنتجات الموجودة وقت العملية إلى «عام».
- اختبر رفع JPG وPNG؛ GIF يبقى كما هو دون ضغط. الصور الجديدة الكبيرة تُحوّل غالبا إلى WebP، ضلع أقصى 2000 بكسل وجودة 0.88 عند تقليل الحجم. افحص صورة ذات تفاصيل تطريز دقيقة قبل اعتماد الإعداد.
- راجع Network: البوابة لا تستدعي Firestore؛ اختيار المتجر يجلب الأقسام فقط؛ المنتجات تُجلب عند فتح القسم.
- جرب فقد الإنترنت ثم إعادة المحاولة.

## ما لا تحتاج تغييره

- أسماء مجموعات Firebase الحالية وروابط الصور القديمة.
- إعدادات Firebase العلنية داخل js/firebase-config.js؛ ليست بديلا عن قواعد الأمان.
- لا تحتاج فهارس مركبة جديدة للمعرض؛ استعلام المنتجات يستخدم mainCategory فقط ويرتب النتيجة محليا.

## ملاحظات مهمة

- مؤقت الخمول في لوحة الإدارة لتحسين سلوك الجلسة، وليس بديلا عن قواعد الأمان أو إلغاء التوكن على الخادم.
- الدخول يستخدم جلسة التبويب بدلا من جلسة دائمة محلية. استعادة جلسة المتصفح قد تختلف؛ استخدم زر خروج عند الانتهاء.
- الكاش في الذاكرة لمدة دقيقة، وليس نسخة محفوظة دائما؛ تحديث البيانات قد لا يظهر في تبويب مفتوح حتى انتهاء الدقيقة وإعادة فتح القسم.
- الصور القديمة لم تُضغط ولم تُنقل؛ بطء ملفاتها الأصلية أو ImgBB قد يبقى عند أول عرض.
- تجنب تعديل اسم القسم نفسه من حسابين في الوقت نفسه. عمليات النقل الكبيرة فوق 450 منتجا تتوقف برسالة واضحة، ولا تُنفذ على دفعات جزئية تلقائيا.
- إذا فشل رفع مجموعة صور بعد نجاح بعضها، قد تبقى صور غير مرتبطة على ImgBB؛ لا تُحذف الصور تلقائيا لتجنب فقد محتوى مقصود.
- روابط إنستقرام وتيك توك كانت # في الأصل؛ ضع روابط حساباتك الفعلية.
- تأكد أن مفاتيح ImgBB القديمة التي سبق نشرها أُلغيت فعليا. إزالة المفتاح من الكود لا تلغيه عند المزود.
- بعد النشر: راقب أخطاء Vercel، واستهلاك Firebase، واضبط تنبيهات الميزانية. App Check يحتاج تهيئة واختبار منفصلين قبل فرضه؛ لم أفعّله تلقائيا حتى لا يتوقف المعرض.

