# إصلاح ERR_REQUIRE_ESM في تسجيل الدخول

السجل المرسل يثبت أن jwks-rsa يحاول require لمكتبة jose ذات صيغة ESM. أعيد إنتاج نفس الخطأ محليا باستخدام node --no-experimental-require-module قبل الإصلاح.

التعديل: override محدود لـ jwks-rsa@4.1.0 لاستخدام jose@5.10.0 الداعم لـCommonJS، مع إعادة توليد package-lock.json. لا يوجد تجاوز للمصادقة أو تغيير للتصميم.

## التركيب فوق النسخة الثالثة
1. استبدل package.json وpackage-lock.json في جذر مستودع FAJER بالملفين المرفقين. لا ترفع ZIP نفسه.
2. انتظر نشر التحديث. إذا أعدت النشر يدويا، ألغ استخدام Build Cache لضمان إعادة تثبيت الاعتماديات.
3. تأكد أن Install Command الافتراضي لم يعطل تثبيت npm؛ يمكن ضبطه على npm ci.
4. افتح صفحة الدخول وأعد المحاولة. لا تغير كلمات المرور أو قواعد Firebase لهذا الخطأ.

نجحت 22 حالة اختبار، منها تحميل Firebase Auth والتحقق من توقيع RSA حقيقي ورفض المحتوى المعدل مع تعطيل require(ESM). هذا يثبت إصلاح التعارض المرسل، ولا يغني عن تجربة الدخول الفعلي بعد النشر. لم ينفذ نشر أو دخول إلى حسابك أثناء العمل.

الـoverride يعوض تعارض التوافق الحالي ويجب مراجعته عند تحديث Firebase Admin أو jwks-rsa مستقبلا.
